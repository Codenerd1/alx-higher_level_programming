#!/usr/bin/node
// A class Rectangle that takes two arguments, width (w) and height (h)
class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}
module.exports = Rectangle;
